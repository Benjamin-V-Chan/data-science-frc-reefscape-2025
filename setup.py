from setuptools import setup, find_packages

setup(
    name="utils",  # Name of the package
    version="0.1",
    packages=find_packages(),  # Automatically finds "utils/"
    install_requires=[],  # List dependencies here if needed
)